name: TestTask
on:
  repository_dispatch:
jobs:
  print:
    name: Print
    runs-on: ubuntu-latest
    steps:
      - name: Print Outputs
        env:
          method: ${{ on.repository_dispatch.outputs.method }}
          body: ${{ toJson(on.repository_dispatch.outputs.body) }}
          headers: ${{ toJson(on.repository_dispatch.outputs.headers) }}
        run: |
          echo method: $method
          echo headers $headers
          echo body: $body